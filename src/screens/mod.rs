//! Top-level Iced pages. Each screen owns its local state and emits `Msg`
//! values the root `App` maps onto service calls.

pub mod add_remote;
pub mod main_page;
pub mod remote_page;
pub mod settings;
